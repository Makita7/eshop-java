spring:
    datasource:
      url: jdbc:mysql://localhost:3306/shoesDataBase
      username: root
      password: root
      driver-class-name: com.mysql.cj.jdbc.Driver
    application:
        name: eshop
    jpa:
      hibernate:
        ddl-auto: create
      database-platform:
        org:
          hibernate:
            dialect: MySQL8Dialect

server:
  port: 8080

