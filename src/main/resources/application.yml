application:
    name: eshop
    username: sof
    password:
      driver-class-name=com:
        mysql:
          cj:
            jdbc:
              Driver:
server:
  port: 8080

jpa:
    hibernate:
      ddl-auto: update
    database-platform:
      org:
        hibernate:
          dialect: MySQL8Dialect